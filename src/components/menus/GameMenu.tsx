import React from "react";
import styled from "styled-components";

type Props = {};

const GameMenuContainer = styled.div`
  display: flex;
  padding: 2%;
`;

const GameMenu = (props: Props) => {
  return <GameMenuContainer>GameMenu</GameMenuContainer>;
};

export default GameMenu;
